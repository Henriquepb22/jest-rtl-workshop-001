import { screen } from '@testing-library/react'

describe('useUser()', () => {
  /*
    Aqui é um belo caso de uso para testar algo que precisa ser encapsulado(Contexts, HOCs) e também as
    chamadas a API.
  */
  it.todo('should return username if user is authenticated')
  it.todo('should login user correctly')
  it.todo('should logout user correctly')
})
