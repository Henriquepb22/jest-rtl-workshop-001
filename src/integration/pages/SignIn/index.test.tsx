import { render, screen } from '@testing-library/react'

describe('<SignIn />', () => {
  it.todo('should render the sign in page with components correctly')
  it.todo('should fill sign in and login correctly')
})
